{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["node_modules"],
  "exec": "ts-node src/app.ts",
  "verbose": true,
  "delay": "1000",
  "events": {
    "restart": "echo 'App is restarting due to changes...'",
    "crash": "echo 'App has crashed! Waiting for changes to restart...' && exit 1"
  }
}

